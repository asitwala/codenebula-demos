const getters = {
  scale(state) {
    return state.scale;
  },
  start(state) {
    return state.start;
  },
  end(state) {
    return state.end;
  },
  useEndAsStart(state) {
    return state.useEndAsStart;
  },
};

export default getters;
